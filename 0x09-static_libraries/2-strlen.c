#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string.
 *
 * @s: the parameters
 *
 * Return: on success,
 *         on error, -1 is returned, and errno is set appropriately
 */

int _strlen(char *s)
{
	return (strlen(s));

}
