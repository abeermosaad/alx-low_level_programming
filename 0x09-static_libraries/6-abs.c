#include <stdio.h>
#include "main.h"

/**
 * _abs - function def
 * @n: paramerter
 * Return: void
 */

int _abs(int n);

/**
 * _abs - function def
 * @n: parameter
 * Return: void
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n);
	}
}
